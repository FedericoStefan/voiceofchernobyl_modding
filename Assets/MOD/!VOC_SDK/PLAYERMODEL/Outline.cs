using UnityEngine;

public class Outline : MonoBehaviour
{
    //Adds support for outlines, as seen in gamemodes with teams when someone is labeled friendly
    
    //You should add this on the animator (like base playermodels) or somewhere that is the parent of all renders (skinned mesh and individual spawned objects)
}
